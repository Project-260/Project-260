import { Button } from '@gnosis.pm/safe-react-components'

const CreateRoleSection = () => {
  return (
    <div>
      <Button size="md">Create New Role</Button>
    </div>
  )
}

export default CreateRoleSection
